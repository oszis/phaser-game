export default class Stage {
    constructor(gameRoot) {
        this.scene = gameRoot;
    }

    init() {
        // todo: тут обязательно должен теряться контекст выполнения, иначе фазер не заведется
    }

    // todo: тут будут различные методы для сцен. Пока что компонент пустой, потому что я хз, что там в итоге будет
}